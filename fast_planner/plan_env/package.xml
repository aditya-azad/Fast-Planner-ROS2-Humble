<?xml version="1.0"?>
<package format="3">
  <name>plan_env</name>
  <version>0.0.0</version>
  <description>The plan_env package (ROS2 port)</description>

  <maintainer email="bzhouai@todo.todo">bzhouai</maintainer>
  <license>LGPL-3.0-or-later</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <build_depend>eigen3_cmake_module</build_depend>
  <build_export_depend>eigen3_cmake_module</build_export_depend>
  <depend>eigen</depend>

  <depend>rclcpp</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>visualization_msgs</depend>
  <depend>cv_bridge</depend>
  <depend>message_filters</depend>
  <depend>pcl_conversions</depend>

  <!-- Optional nvblox support - handled in CMakeLists.txt via find_package(QUIET) -->
  <!-- nvblox_msgs is optional and will be detected at build time -->

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
